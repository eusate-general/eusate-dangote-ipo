@AGENTS.md

Read PLAN.md before changing anything: it holds the product contract (what the bot must and must not do), the freshness design, and the reasons behind the architecture. Facts live in `facts/` and are validated at build time. Do not put unverified claims into `facts/ipo.yaml` as `confirmed`.
